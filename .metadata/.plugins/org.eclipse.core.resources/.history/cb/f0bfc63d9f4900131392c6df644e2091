package reflection.utilClasses;

import reflection.interfaces.Strategy;

public class ComplexType implements Strategy{

	@Override
	public boolean check(String s) {
		if(fileData.trim().contains("complexType")){
			return true;
		}
		return false;
	}

}
